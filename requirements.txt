# Web stack (already present in the system interpreter; pinned for reproducibility)
fastapi>=0.128
uvicorn[standard]>=0.39
httpx>=0.28
pydantic>=2.13
python-dotenv>=1.2
tenacity>=9.1
jinja2>=3.1

# New runtime deps
anthropic>=0.40
aiosqlite>=0.20

# Dev / tests
pytest>=8
pytest-asyncio>=0.23
